# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/

# HF / data caches
data_cache/
.cache/

# Model outputs
flan-t5-meddialog-finetuned/
trainer_output/
smoke_output/
smoke_model/

# Smoke-run artifacts (dev only)
smoke_test_results.csv
smoke_eval.json
# Note: test_results.csv + evaluation_results.json + run.log from the real
# run are tracked — they are presentation deliverables.

# Raw dataset dumps (large, not tracked)
train_data.json
validate_data.json
test_data.json

# OS
.DS_Store
